import React from 'react';

export default () => {
	return <div>List of blog posts (IndexRoute renders this with App.js, when '/' path is requested. Any other path will not render posts_index.js)</div>;
};